# Claude Code ガイドライン

## PR作成ルール

- `git push` やPR作成など、外部リポジトリに影響する操作は **必ずユーザーの許可を得てから実行する**
- PR本文は `.github/pull_request_template.md` のテンプレートを参照し、git の変更履歴・コミットメッセージをもとに各セクションを埋める
- PRタイトルは70文字以内、日本語で簡潔に記載する
